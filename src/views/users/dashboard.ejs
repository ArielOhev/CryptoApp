<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Crypto Dashboard</title>
</head>
<body>
    <div style="display: flex; justify-content: center;">
        <form style="display: flex; flex-direction: column; height: 400px; width: 400px;" method="POST" action="/users/symbol/add">
            <input action="text" name="symbol" placeholder="crypto token">
            <button class ="btn" style="border-radius: 100px;">Add</button>
        </form>
    </div>
    
    <table>
        <thead>
            <tr>
                <th>Symbol</th>
                <th>Value</th>
            </tr>
        </thead>
        <tbody>
            <% userSymbols.forEach(userSymbols=>{%>
                <tr>
                    <td><%= userSymbols.symbol %></td>
                    <td></td>
                </tr>
            <%}) %>
        </tbody>
    </table>
    
</body>
</html>